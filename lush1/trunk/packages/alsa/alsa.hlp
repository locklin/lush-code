#? **** Advanced Linux Sound Architecture (ALSA)

This is a full interface to the ALSA API.
The ALSA API allows such thing a sound recording, playing,
compression, mixing, midi, and some sound processing.

#? *** << alsa/alsa-config.lsh
#? *** << alsa/pcm.lsh
#? *** << alsa/rawmidi.lsh
#? *** << alsa/seq.lsh

